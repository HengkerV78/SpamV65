# Install in Termux
```bash
apt update && apt upgrade -y
apt install nodejs git -y
apt install yarn -y
git clone https://github.com/HengkerV78/SpamV65.git
cd SpamV65
npm install
yarn install
npm start
